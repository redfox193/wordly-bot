services:
  db:
    image: postgres:16-alpine
    env_file: .env
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data

  bot:
    build: ./bot
    env_file: .env
    depends_on:
      db:
        condition: service_started

volumes:
  pgdata:
